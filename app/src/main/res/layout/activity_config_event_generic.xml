<?xml version="1.0" encoding="utf-8"?>
<!--
  Generic config layout for multi-field event plugins (E3, E5-E15). Neon-Grid themed.
  The Activity sets pbTitle/pbDesc in code and programmatically adds one Widget.NeonGrid.TextInputLayout
  + TextInputEditText per filter field into pbFieldsContainer. attachVariablePickers() (run by the base
  after onConfigCreated) then adds the variable picker end-icon to every field so %vars + Tasker
  suggestions work uniformly. A watch-browse button is included but hidden unless the Activity shows it.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/pbTitle"
            style="@style/TextAppearance.NeonGrid.HeadlineSmall"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:ignore="HardcodedText"
            android:text="Pebble event" />

        <TextView
            android:id="@+id/pbDesc"
            style="@style/TextAppearance.NeonGrid.BodySmall"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="16dp"
            tools:ignore="HardcodedText"
            android:text="" />

        <com.google.android.material.card.MaterialCardView
            style="@style/Widget.NeonGrid.CardView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <LinearLayout
                    android:id="@+id/pbFieldsContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/pbBtnPickWatch"
                    style="@style/Widget.NeonGrid.Button.Text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="gone"
                    android:text="@string/pb_evt_pick_watch" />

                <TextView
                    android:id="@+id/pbOutputs"
                    style="@style/TextAppearance.NeonGrid.BodySmall"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="@string/pb_outputs_hint" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>
</ScrollView>
